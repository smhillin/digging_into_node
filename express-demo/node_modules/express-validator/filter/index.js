const {
  buildSanitizeFunction,
  sanitize,
  sanitizeBody,
  sanitizeCookie,
  sanitizeParam,
  sanitizeQuery
} = require('express-validator/filter/sanitization-chain-builders');

module.exports = {
  buildSanitizeFunction,
  sanitize,
  sanitizeBody,
  sanitizeCookie,
  sanitizeParam,
  sanitizeQuery,
  matchedData: require('express-validator/filter/matched-data')
};